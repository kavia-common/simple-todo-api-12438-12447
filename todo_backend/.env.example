# Backend environment example for todo_backend (Flask)

# Required PostgreSQL variables (provided by the todo_database container)
POSTGRES_URL=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=postgres

# Optional: standard DATABASE_URL override (not required)
# DATABASE_URL=postgresql+psycopg://postgres:your_password_here@127.0.0.1:5432/postgres

# Optional diagnostics and DB init tuning
BACKEND_DEBUG=true
DB_CONNECT_TIMEOUT=5
DB_INIT_RETRIES=5
DB_INIT_RETRY_DELAY=2.0

# Flask server port (the app binds to 0.0.0.0:3001 by default in run.py)
# Change only if your environment requires a different port
# FLASK_RUN_PORT=3001
